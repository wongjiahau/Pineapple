def (this String).replace(old String with that String) -> String
    return this.splitBy(old).joinBy(that)

def (this String).splitBy(that String) -> String
    <javascript>
    return $this.split($that);
    </javascript>

def (this String).joinBy(that String) -> String
    <javascript>
    return $this.join($that)
    </javascript>