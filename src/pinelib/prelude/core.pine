import "./boolean.pine"

def Nil
    #nil

def Undefined
    #undefined

def (this Any) == (that Any) -> Boolean
    <javascript>
    return $this === $that;
    </javascript>

def (this Any) =/= (that Any) -> Boolean
    <javascript>
    return $this !== $that;
    </javascript>

def (this String).toInteger -> Integer
    <javascript>
    return parseInteger($this);
    </javascript>

def (this Any).toString -> String
    <javascript>
    return $this.toString();
    </javascript>