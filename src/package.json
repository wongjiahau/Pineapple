{
  "name": "src",
  "version": "1.0.0",
  "description": "``` npm i -g jison --save ```",
  "main": "pineapple-parser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.10.0"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
