import { expect } from "chai";
// @ts-ignore
import * as mocha from "mocha";

describe("Prelude", () => {
    it("Just for importing mocha", () => {
        expect(true).to.equals(true);
    });

});
