import { interpret } from "../../interpret";

const fs = require("fs")
describe("interpreter", () => {
    it("case 1", () => {
        // console.log(fs.readFileSync("../hi.pine"))
        // interpret("./hi.pine", (s: string) => {});
    });
});
