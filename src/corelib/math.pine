def (this Number) + (that Number) -> Int
    <javascript>
    return $this + $that;
    </javascript>

def (this Number) < (that Number) -> Boolean
    <javascript>
    return $this < $that;
    </javascript>

